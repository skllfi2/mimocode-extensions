{
  "mcpServers": {
    "filesystem": {
      "type": "local",
      "command": ["npx", "-y", "@modelcontextprotocol/server-filesystem"],
      "description": "File system operations with configurable access controls"
    },
    "memory": {
      "type": "local",
      "command": ["npx", "-y", "@modelcontextprotocol/server-memory"],
      "description": "Knowledge graph-based persistent memory system"
    },
    "github": {
      "type": "local",
      "command": ["npx", "-y", "@bitofacoder/omni-mcp-server"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "your-token-here"
      },
      "description": "GitHub API, local Git, Slack, web fetch, memory, and filesystem (all-in-one)"
    },
    "sequential-thinking": {
      "type": "local",
      "command": ["npx", "-y", "@modelcontextprotocol/server-sequential-thinking"],
      "description": "Dynamic and reflective problem-solving through thought sequences"
    },
    "postgres": {
      "type": "local",
      "command": ["npx", "-y", "@ahmetkca/mcp-server-postgres"],
      "env": {
        "DATABASE_URL": "postgresql://user:pass@localhost/db"
      },
      "description": "PostgreSQL database access (read-only, multi-tenant, schema introspection)"
    },
    "puppeteer": {
      "type": "local",
      "command": ["npx", "-y", "@hisma/server-puppeteer"],
      "description": "Browser automation and web scraping (Puppeteer fork)"
    }
  }
}
