<Page
    x:Class="MimoLauncher.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:MimoLauncher.Models"
    xmlns:converters="using:MimoLauncher.Converters"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <converters:NullToFalseConverter x:Key="NullToFalseConverter"/>
    </Page.Resources>

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="MiMoCode Launcher"
                       Style="{StaticResource TitleTextBlockStyle}"
                       Margin="0,0,0,16"/>

            <!-- Search Box -->
            <AutoSuggestBox x:Name="SearchBox"
                           PlaceholderText="Search projects..."
                           QueryIcon="Find"
                           Width="400"
                           HorizontalAlignment="Left"
                           Text="{x:Bind ViewModel.SearchText, Mode=TwoWay}"
                           QuerySubmitted="SearchBox_QuerySubmitted"/>
        </StackPanel>

        <!-- Project List -->
        <ListView x:Name="ProjectList"
                  Grid.Row="1"
                  SelectionMode="Single"
                  IsItemClickEnabled="True"
                  ItemClick="ProjectList_ItemClick"
                  SelectionChanged="ProjectList_SelectionChanged"
                  ItemsSource="{x:Bind ViewModel.Projects}">

            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:Project">
                    <Grid Padding="16,12" CornerRadius="8"
                          Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{x:Bind Name}"
                                       Style="{StaticResource BodyStrongTextBlockStyle}"
                                       Margin="0,0,0,4"/>
                            <TextBlock Text="{x:Bind Description}"
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       TextTrimming="CharacterEllipsis"
                                       MaxLines="2"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" HorizontalAlignment="Right" Margin="16,0,0,0">
                            <TextBlock Text="{x:Bind Category}"
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       HorizontalAlignment="Right"/>
                            <TextBlock Text="{x:Bind LastUsed}"
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                       HorizontalAlignment="Right"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Footer Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,24,0,0">
            <Button Content="Add Project"
                    Style="{StaticResource AccentButtonStyle}"
                    Click="AddProject_Click"
                    Margin="0,0,8,0"/>

            <Button Content="Launch MiMoCode"
                    Style="{StaticResource AccentButtonStyle}"
                    Click="LaunchMiMoCode_Click"
                    IsEnabled="{x:Bind ViewModel.SelectedProject, Converter={StaticResource NullToFalseConverter}}"/>

            <Button Content="Open Folder"
                    Click="OpenFolder_Click"
                    IsEnabled="{x:Bind ViewModel.SelectedProject, Converter={StaticResource NullToFalseConverter}}"
                    Margin="8,0,0,0"/>

            <Button Content="Delete"
                    Click="DeleteProject_Click"
                    IsEnabled="{x:Bind ViewModel.SelectedProject, Converter={StaticResource NullToFalseConverter}}"
                    Margin="8,0,0,0"
                    Foreground="{ThemeResource SystemFillColorCriticalBrush}"/>

            <TextBlock x:Name="StatusTextBlock"
                       Text="{x:Bind ViewModel.StatusText, Mode=OneWay}"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       VerticalAlignment="Center"
                       Margin="24,0,0,0"/>
        </StackPanel>
    </Grid>
</Page>
